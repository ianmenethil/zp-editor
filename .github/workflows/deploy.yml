name: Deploy

on:
  push:
    branches:
      - main

env:
  NODE_VERSION: 24.x

permissions:
  contents: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    env:
      NODE_OPTIONS: "--max_old_space_size=4096"
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: "npm"
          cache-dependency-path: "**/package-lock.json"

      - name: Install dependencies
        run: npm ci

      - name: Deploy
        run: |
          git remote set-url origin https://git:${GITHUB_TOKEN}@github.com/${{github.repository}}.git
          npm run deploy -- -u "github-actions-bot <support+actions@github.com>"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SELF_HOSTED: "true"
          FIREBASE_CONFIG: '{"apiKey":"AIzaSyBBjBFx10SJcNSc9fdr6kVqcx3ydml0QMk","authDomain":"livecodes-43380.firebaseapp.com","projectId":"livecodes-43380","storageBucket":"livecodes-43380.firebasestorage.app","messagingSenderId":"356498821161","appId":"1:356498821161:web:a828a7eae68a936fc68c38"}'
          SANDBOX_HOST_NAME: "livecodes-sandbox.pages.dev"
          SANDBOX_PORT: "443"
